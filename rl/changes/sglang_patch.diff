diff --git a/python/sglang/srt/managers/multimodal_processors/qwen_vl.py b/python/sglang/srt/managers/multimodal_processors/qwen_vl.py
index 962a2acd..4a89a276 100644
--- a/python/sglang/srt/managers/multimodal_processors/qwen_vl.py
+++ b/python/sglang/srt/managers/multimodal_processors/qwen_vl.py
@@ -1,3 +1,4 @@
+import os
 import asyncio
 import math
 from typing import List, Union
@@ -33,7 +34,7 @@ class Qwen2_5VLImageProcessor(SGLangBaseProcessor):
         self.NUM_TOKEN_PER_FRAME = 770
         self.IMAGE_FACTOR = 28
         self.MIN_PIXELS = 4 * 28 * 28
-        self.MAX_PIXELS = 16384 * 28 * 28
+        self.MAX_PIXELS = int(os.environ.get('MAX_PIXELS', 16384 * 28 * 28))
         self.MAX_RATIO = 200
 
     async def process_mm_data_async(
diff --git a/python/sglang/srt/managers/tokenizer_manager.py b/python/sglang/srt/managers/tokenizer_manager.py
index 82709b09..e72bb8fc 100644
--- a/python/sglang/srt/managers/tokenizer_manager.py
+++ b/python/sglang/srt/managers/tokenizer_manager.py
@@ -24,9 +24,11 @@ import sys
 import threading
 import time
 import uuid
+import random
 from collections import deque
 from datetime import datetime
 from http import HTTPStatus
+import gc
 from typing import (
     Any,
     Awaitable,
@@ -382,6 +384,9 @@ class TokenizerManager:
             if is_single:
                 tokenized_obj = await self._tokenize_one_request(obj)
                 self._send_one_request(obj, tokenized_obj, created_time)
+                # this variable tokenized_obj contains many images and takes up a lot of memory
+                del tokenized_obj
+                if random.random() < 0.2: gc.collect()
                 async for response in self._wait_one_response(obj, request):
                     yield response
             else:
diff --git a/python/sglang/srt/models/qwen2_5_vl.py b/python/sglang/srt/models/qwen2_5_vl.py
index 17697a83..04d01cd3 100644
--- a/python/sglang/srt/models/qwen2_5_vl.py
+++ b/python/sglang/srt/models/qwen2_5_vl.py
@@ -496,11 +496,32 @@ class Qwen2_5_VLForConditionalGeneration(nn.Module):
         # in qwen-vl, last dim is the same
         pixel_values = torch.cat([item.pixel_values for item in items], dim=0).type(
             self.visual.dtype
-        )
+        ).to(self.visual.device)
         image_grid_thws = torch.concat([item.image_grid_thws for item in items], dim=0)
+
+        # delete item.pixel_values to save memory
+        for item in items:
+            item.pixel_values = None
+
+        # print(f"{pixel_values.shape=}, {image_grid_thws.shape=}, {image_grid_thws.tolist()=}")
         assert pixel_values.dim() == 2, pixel_values.dim()
         assert image_grid_thws.dim() == 2, image_grid_thws.dim()
-        image_embeds = self.visual(pixel_values, grid_thw=image_grid_thws)
+
+        # split image encoding into smaller batches to prevent oom
+        batch_size = 8      # fewer images at a time
+        pixel_values_start_idx = 0
+        image_embeds = list()
+        for i in range(0, image_grid_thws.size(0), batch_size):
+            image_grid_thws_batch = image_grid_thws[i : i + batch_size]
+            num_patches = sum([i.prod() for i in image_grid_thws_batch])
+            pixel_values_batch = pixel_values[pixel_values_start_idx : pixel_values_start_idx + num_patches]
+            image_embeds_batch = self.visual(pixel_values_batch, grid_thw=image_grid_thws_batch)
+            image_embeds.append(image_embeds_batch)
+            pixel_values_start_idx += num_patches
+        assert pixel_values_start_idx == pixel_values.size(0), f"Some of the images are not used! {pixel_values.size()=}, {image_grid_thws.tolist()=}"
+        image_embeds = torch.cat(image_embeds, dim=0)
+
+        # image_embeds = self.visual(pixel_values, grid_thw=image_grid_thws)
         return image_embeds
 
     def _process_video_input(self, video_input: Qwen2VLVideoInputs) -> torch.Tensor:
