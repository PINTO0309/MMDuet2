diff --git a/.gitignore b/.gitignore
index 8dc4dcb..010c595 100644
--- a/.gitignore
+++ b/.gitignore
@@ -8,6 +8,7 @@
 **/*.tar.gz
 **/playground
 **/wandb
+**/tensorboard_log
 
 # Byte-compiled / optimized / DLL files
 __pycache__/
diff --git a/recipe/proactive/reward_function.py b/recipe/proactive/reward_function.py
new file mode 100644
index 0000000..e1a72a4
--- /dev/null
+++ b/recipe/proactive/reward_function.py
@@ -0,0 +1,377 @@
+import random
+import requests
+import os
+import re
+import time
+import copy
+import logging
+import concurrent.futures
+import os
+
+logger = logging.getLogger(__file__)
+logger.setLevel(logging.INFO)
+
+URL = os.environ.get("LLM_EVALUATOR_URL", "http://localhost:30000/v1/chat/completions")
+NO_REPLY_STR = 'NO REPLY'
+API_KEY = os.environ.get("LLM_EVALUATOR_API_KEY", None)
+MODEL = os.environ.get("LLM_EVALUATOR_MODEL", "Qwen/Qwen3-32B")
+MODEL_REPETITION = os.environ.get("LLM_EVALUATOR_MODEL_REPETITION", MODEL)
+MAX_SCORES = int(os.environ.get("LLM_EVALUATOR_MAX_SCORES", 5))
+PAUC_ADD_PREV_TURNS = int(os.environ.get("LLM_EVALUATOR_PAUC_ADD_PREV_TURNS", 0))  # if or not add prev replies in the same ground truth reply span when calculating similarity
+REPETITION_IN_ALL_SPANS = int(os.environ.get("LLM_EVALUATOR_REPETITION_IN_ALL_SPANS", 1))   # if or not add prev replies in previous ground truth reply spans when calculating repetition
+
+PAUC_WEIGHT = float(os.environ.get("LLM_EVALUATOR_PAUC_WEIGHT", 3))
+REPETITION_WEIGHT = float(os.environ.get("LLM_EVALUATOR_REPETITION_WEIGHT", 3))
+OUTSPAN_WEIGHT = float(os.environ.get("LLM_EVALUATOR_OUTSPAN_WEIGHT", 2))
+COMMON_PREFIX_WEIGHT = float(os.environ.get("LLM_EVALUATOR_COMMON_PREFIX_WEIGHT", 0))
+COMMON_PREFIX_THRES = float(os.environ.get("LLM_EVALUATOR_COMMON_PREFIX_THRES", 50))
+
+logger.info(f"reward function info: {MODEL=}, {MODEL_REPETITION=}, {URL=}, {MAX_SCORES=}, {PAUC_ADD_PREV_TURNS=}, {REPETITION_IN_ALL_SPANS=}, {COMMON_PREFIX_THRES=}")
+logger.info(f"{PAUC_WEIGHT=}, {REPETITION_WEIGHT=}, {OUTSPAN_WEIGHT=}, {COMMON_PREFIX_WEIGHT=}")
+
+
+similarity_instruction_3scores = (
+    "You are an evaluator for a video question answering system. Your task is to rate the whether the predicted answer covers the key points of the ground truth answer. Use the following scale to assign a score:\n"
+    "- 3: Mostly covered; the predicted answer covers all key information in the ground truth answer, though it may have minor inaccuracies or rephrases.\n"
+    "- 2: Partially covered; the predicted answer has some correct information, but also contains significant inaccuracies or missing key points.\n"
+    "- 1: Incorrect; the predicted answer may be related to the ground truth answer, but most of the information is missing, or the predicted answer is not relevant to the question or in very poor quality.\n"
+    "Output the score only, do not add more explanations."
+)
+
+similarity_instruction_5scores = (
+    "You are an evaluator for a video question answering system. Your task is to rate whether the predicted answer covers the key points of the ground truth answer. Use the following scale to assign a score:\n"
+    "- 5: Fully covered; the predicted answer covers all key information in the ground truth answer with no significant inaccuracies or rephrasing.\n"
+    "- 4: Mostly covered; the predicted answer covers all key information but may have minor inaccuracies or rephrasing.\n"
+    "- 3: Partially covered; the predicted answer includes some correct information but is missing key points or has significant inaccuracies.\n"
+    "- 2: Largely incorrect; the predicted answer contains some relevant information but is mostly inaccurate or irrelevant.\n"
+    "- 1: Incorrect; the predicted answer is either not relevant to the question or very poor in quality.\n"
+    "Output the score only, do not add more explanations."
+)
+
+similarity_instruction = {
+    3: similarity_instruction_3scores,
+    5: similarity_instruction_5scores
+}
+
+repetition_instruction = (
+    "You are an evaluator for a multi-turn video question answering system. Your task is to judge whether a new reply generated by the model is repetitive (all its information is already convered by previous turns) or not.\n"
+    "Output \"yes\" if all information in the new turn is already covered by previous turns. Output \"no\" if there is new information in the new turn.\n"
+    "Only reply with \"yes\" or \"no\". Do not add any more explainations."
+)
+
+has_any_repetition_instruction = (
+    "You are an evaluator for a multi-turn video question answering system. Your task is to judge whether a new reply generated by the model has some content that repeats the previous turns.\n"
+    "Output \"yes\" if some of the content in this turn is repeating the previous turns. Output \"no\" if all the information in this turn is substantially different from the previous turns.\n"
+    "Only reply with \"yes\" or \"no\". Do not add any more explainations."
+)
+
+
+def send_request(model_input, instruction, model=MODEL):
+    if "Qwen3" in model:
+        model_input += "/no_think"     # non-thinking mode for qwen-3
+
+    data = {
+        "model": model,
+        "messages": [
+            {'role': 'system', 'content': instruction},
+            {'role': 'user', 'content': model_input},
+        ],
+    }
+
+    if "gemini-2.5" in model:
+        data['reasoning_effort'] = 'low'   # this is the minimal thinking budget for gemini
+
+    if API_KEY is not None:
+        headers = {'Authorization': f'Bearer {API_KEY}', 'Content-Type': 'application/json'}
+    else:
+        headers = None
+
+    max_retries = 3
+    retry_count = 0
+    while retry_count < max_retries:
+        try:
+            response = requests.post(URL, json=data, headers=headers)
+            response.raise_for_status()
+            model_output = response.json()['choices'][0]['message']['content']
+            model_output = model_output.replace('<think>', '').replace('</think>', '').strip()
+            return model_output
+        except Exception as e:
+            retry_count += 1
+            if retry_count < max_retries:
+                logger.error(f"Error in calculate_similarity_score (attempt {retry_count}/{max_retries}): {e}")
+                time.sleep(2)
+            else:
+                logger.error(f"Error in calculate_similarity_score (final attempt): {e}")
+                return None
+
+
+def calculate_similarity_score(time, question, pred, gold):
+    model_input = (
+        f"Question: {question}\n"
+        f"Ground Truth Answer: {gold}\n"
+        f"Predicted Answer: {pred}\n"
+    )
+
+    model_output = send_request(model_input, similarity_instruction[MAX_SCORES], model=MODEL)
+    if model_output in ['5', '4', '3', '2', '1']:
+        score = int(model_output) - 1
+    else:
+        score = None        # failing requests does not count into the reward
+    return time, score
+
+
+def calculate_is_repetition(time, previous_sents, pred_sent):
+    model_input = (
+        f"Previous Turns: {previous_sents}\n"
+        f"New Turn: {pred_sent}\n"
+    )
+    model_output = send_request(model_input, repetition_instruction, model=MODEL_REPETITION)
+    res = None if model_output is None else model_output.lower().startswith('yes')
+    return time, res
+
+
+def calculate_have_common_prefix(time, previous_sents, pred_sent):
+    for prev_sent in previous_sents:
+        common_prefix = os.path.commonprefix([prev_sent, pred_sent])
+        if len(common_prefix) >= COMMON_PREFIX_THRES:
+            return time, 1
+    return time, 0
+
+
+def parse_solution_str(solution_str):
+    if not solution_str.startswith('assistant\n'):
+        solution_str = 'assistant\n' + solution_str
+    if not solution_str.endswith('\nuser'):
+        solution_str += '\nuser'
+
+    pattern = r'(?<=assistant\n)(.*?)(?=\nuser|$)'
+    responses = re.findall(pattern, solution_str, re.DOTALL)
+    return [r.strip() for r in responses]
+
+
+def area_under_line_ratio(points, max_x, max_y, floor_time_score_weight=0.5, start_score=0.5):
+    """
+    计算坐标点组成的折线与 x 轴围成的面积
+    值得注意的是，这里相邻的两个点并不是直接连一个斜线过去，而是在后一个点x_2处，y值忽然激增到y_2。
+
+    :param points: 列表，包含 (x, y) 元组，例如 [(x1, y1), (x2, y2), ...]
+    :return: 面积（浮点数）
+    """
+    if not len(points): return start_score / max_y
+    points = sorted(points, key=lambda p: p[0])  # 按 x 坐标排序（确保从左到右计算）
+
+    # 即使我在span的最后生成回复，也被认定为获得了`floor_time_score_weight=0.5` of all 时间分。
+    # 不然的话，如果模型恰好在span的结尾才生成回复，时间分就几乎为0，这导致最终整体分数也几乎为0了。
+    # 而这个接近span的结尾才生成的答案显然也是有价值的。``
+    # 这个超参数如果是0，则不对横轴进行修改，是timeliness比correctness更重要的情景。
+    # 这个超参数如果是1，则将所有点的横轴平移到1。是correctness比timeliness更重要的情景。
+
+    # if floor_time_score_weight == 1: floor_time_score_weight = 0.99     # 为了防止所有点在x轴上重合在一起，并不能真的设定成1，改成0.99就够
+    points = [(x * (1 - floor_time_score_weight), y) for x, y in points]
+    points.append([max_x, points[-1][1]])
+    prev_y, prev_x, area = start_score, 0, 0
+    max_area = max_x * max_y
+    for i in range(len(points)):
+        x1, y1 = points[i]
+        area += (x1 - prev_x) * prev_y
+        prev_x, prev_y = x1, y1
+    return area / max_area
+
+
+def pauc_reward_function(data_source, solution_str, ground_truth, extra_info=None):
+    reply_timestamps = ground_truth['model_pred_timestamps']
+    assistant_replies_in_prompt = list(ground_truth.get('prev_assistant_replies', list()))
+    max_score = MAX_SCORES - 1
+    pred_data = list(zip(reply_timestamps, parse_solution_str(solution_str)))
+
+    # gather the commands that  should be executed
+    similarity_args, repetition_args = list(), list()
+    if REPETITION_IN_ALL_SPANS:
+        added_pred_sents_all_spans = copy.copy(assistant_replies_in_prompt)
+        for time, pred_sent in pred_data:
+            if pred_sent == NO_REPLY_STR: continue
+            if pred_sent in added_pred_sents_all_spans: continue
+            added_pred_sents_all_spans.append(pred_sent)
+            repetition_args.append((time, ' '.join(added_pred_sents_all_spans[:-1]), pred_sent))
+
+    have_common_prefix = list()
+    for sent_i in range(len(pred_data)):
+        prev_sents_in_pred_data = [e[1] for e in pred_data[:sent_i]]
+        have_common_prefix.append(
+            calculate_have_common_prefix(
+                pred_data[sent_i][0],  prev_sents_in_pred_data + assistant_replies_in_prompt, pred_data[sent_i][1]))
+    reward_common_prefix = -1 * (sum([e[1] for e in have_common_prefix]) / len([e[1] for e in have_common_prefix]) if have_common_prefix else 0)
+
+
+    inspan_reply_times = set()
+    for answer_turn in ground_truth['answer']:
+        if answer_turn['reply_timespan'][0] > pred_data[-1][0]:
+            # this answer turn is totally truncated as input is too long
+            continue
+        all_pred_sents = [(time, pred_sent) for time, pred_sent in pred_data \
+                    if answer_turn['reply_timespan'][0] <= time <= answer_turn['reply_timespan'][1]]
+        inspan_reply_times.update([time for time, _ in all_pred_sents])
+        added_pred_sents = []
+        points = list()
+        for time, pred_sent in all_pred_sents:
+            if pred_sent == NO_REPLY_STR: continue
+            if pred_sent in added_pred_sents:
+                continue
+
+            added_pred_sents.append(pred_sent)
+
+            if PAUC_ADD_PREV_TURNS:
+                similarity_args.append((time, answer_turn['question'], ' '.join(added_pred_sents), answer_turn['content']))
+            else:
+                similarity_args.append((time, answer_turn['question'], pred_sent, answer_turn['content']))
+
+            if not REPETITION_IN_ALL_SPANS and len(added_pred_sents) > 1:
+                repetition_args.append((time, ' '.join(added_pred_sents[:-1]), pred_sent))
+
+    # out-of-span replies
+    outspan_times = [(time, pred_sent) for time, pred_sent in pred_data if time not in inspan_reply_times]
+    outspan_replies = [(time, pred_sent) for time, pred_sent in pred_data if time not in inspan_reply_times and pred_sent != NO_REPLY_STR]
+    reward_outspan = -1 * (len(outspan_replies) / len(outspan_times) if outspan_times else 0)
+
+    # execute the commands in parallel
+    # DEPRECATED: i moved the parallelism code to verl/verl/workers/reward_manager/naive.py
+    # with concurrent.futures.ThreadPoolExecutor(max_workers=1) as executor:
+    #     if PAUC_WEIGHT != 0:
+    #         similarity_futures = [executor.submit(calculate_similarity_score, *args) for args in similarity_args]
+    #     if REPETITION_WEIGHT != 0:
+    #        repetition_futures = [executor.submit(calculate_is_repetition, *args) for args in repetition_args]
+
+    #     if PAUC_WEIGHT != 0:
+    #         similarity_results = [future.result() for future in concurrent.futures.as_completed(similarity_futures)]
+    #         similarity_results_dict = dict(similarity_results)
+    #     else:
+    #         similarity_results_dict = dict()
+    #     if REPETITION_WEIGHT != 0:
+    #         repetition_results = [future.result() for future in concurrent.futures.as_completed(repetition_futures)]
+    #         repetition_results_dict = dict(repetition_results)
+    #     else:
+    #         repetition_results_dict = dict()
+
+    if PAUC_WEIGHT != 0:
+        similarity_results = [calculate_similarity_score(*args) for args in similarity_args]
+        similarity_results_dict = dict(similarity_results)
+    else:
+        similarity_results_dict = dict()
+
+    if REPETITION_WEIGHT != 0:
+        repetition_results = [calculate_is_repetition(*args) for args in repetition_args]
+        repetition_results_dict = dict(repetition_results)
+    else:
+        repetition_results_dict = dict()
+
+    # read the scores from the dict
+    auc_list, repetition_list = list(), list()
+
+    if REPETITION_IN_ALL_SPANS:
+        added_pred_sents_all_spans = assistant_replies_in_prompt
+        for time, pred_sent in pred_data:
+            if pred_sent == NO_REPLY_STR: continue
+            if pred_sent in added_pred_sents_all_spans:
+                repetition_list.append(1)
+                continue
+            added_pred_sents_all_spans.append(pred_sent)
+            if repetition_results_dict.get(time, None) is not None:
+                repetition_list.append(repetition_results_dict[time])
+
+    for answer_turn in ground_truth['answer']:
+        if answer_turn['reply_timespan'][0] > pred_data[-1][0]:
+            # this answer turn is totally truncated as input is too long
+            continue
+        all_pred_sents = [(time, pred_sent) for time, pred_sent in pred_data \
+                    if answer_turn['reply_timespan'][0] <= time <= answer_turn['reply_timespan'][1]]
+        added_pred_sents = list()
+        points = list()
+        for time, pred_sent in all_pred_sents:
+            if pred_sent == NO_REPLY_STR: continue
+
+            if not REPETITION_IN_ALL_SPANS and pred_sent in added_pred_sents and REPETITION_WEIGHT != 0:
+                repetition_list.append(1)
+
+            if pred_sent in added_pred_sents:
+                continue
+            added_pred_sents.append(pred_sent)
+
+            score = similarity_results_dict.get(time, None)
+            if score is not None:
+                points.append((time - answer_turn['reply_timespan'][0], score))
+
+            if len(added_pred_sents) != 1 and repetition_results_dict.get(time, None) is not None:
+                repetition_list.append(repetition_results_dict[time])
+
+        max_x, max_y = answer_turn['reply_timespan'][1] - answer_turn['reply_timespan'][0], max_score
+        auc_list.append(area_under_line_ratio(points, max_x, max_y))
+
+    reward_auc = sum(auc_list) / len(auc_list) if len(auc_list) else 0
+    reward_repetition = -1 * (sum(repetition_list) / len(repetition_list) if len(repetition_list) else 0)
+
+    # random sample 1/10 of the results for debug print
+    if hash(ground_truth['question_id']) % 10 == 0:
+        print("ground_truth:", ground_truth)
+        print('parsed solution:', parse_solution_str(solution_str))
+        print('extra_info:', extra_info)
+        print(f'{similarity_results_dict=} {repetition_results_dict=} {outspan_times=} {outspan_replies=} {have_common_prefix=}')
+        print(f'{reward_auc=} {reward_repetition=} {reward_outspan=} {reward_common_prefix=}')
+    return PAUC_WEIGHT * reward_auc + REPETITION_WEIGHT * reward_repetition + \
+        OUTSPAN_WEIGHT * reward_outspan + COMMON_PREFIX_WEIGHT * reward_common_prefix
+
+
+def debug_reward_function(data_source, solution_str, ground_truth, extra_info=None):
+    print("ground_truth:", ground_truth)
+    print('parsed solution:', parse_solution_str(solution_str))
+    print('extra_info:', extra_info)
+    return random.random()
+
+
+def compute_score(*args, **kwargs):
+    return pauc_reward_function(*args, **kwargs)
+
+
+if __name__ == "__main__":
+    solution_str = """NO REPLY
+user
+What activity is the young blonde woman engaged in?
+assistant
+The young blonde woman is seemingly involved in a conversation or video call.
+user
+
+assistant
+NO REPLY
+user
+
+assistant
+NO REPLY
+user
+
+assistant
+The woman is brushing her teeth with a toothbrush.
+user
+
+assistant
+The woman is brushing her teeth with a toothbrush, and holding a toothpaste.
+user
+
+assistant
+NO REPLY
+user
+
+assistant
+NO REPLY"""
+
+    ground_truth = {
+        'answer': [
+            # {'content': 'The woman is preparing to brush her teeth by holding a toothpaste and a toothbrush.', 'reply_timespan': [3.44, 9.84], 'question': 'What is the woman doing?'},
+            {'content': 'The woman is having a conversation.', 'reply_timespan': [1, 3], 'question': 'What is the woman doing?'},
+            {'content': 'The woman is brushing her teeth.', 'reply_timespan': [3, 9], 'question': 'What is the woman doing?'},
+        ],
+        'question_id': 'd0zI4QDJNA0.63.mp4',
+        'model_pred_timestamps': [1, 2, 3, 4, 5, 6, 7, 8],
+        'prev_assistant_replies': ['The woman is brushing her teeth with toothbrush.']
+    }
+
+    print(parse_solution_str(solution_str))
+    print(pauc_reward_function(None, solution_str, ground_truth))
diff --git a/recipe/proactive/run_scripts/train_grpo-half_multi_half_single_question-1_sec-20s_seg_as_example-max_180_frames.sh b/recipe/proactive/run_scripts/train_grpo-half_multi_half_single_question-1_sec-20s_seg_as_example-max_180_frames.sh
new file mode 100644
index 0000000..db29667
--- /dev/null
+++ b/recipe/proactive/run_scripts/train_grpo-half_multi_half_single_question-1_sec-20s_seg_as_example-max_180_frames.sh
@@ -0,0 +1,121 @@
+# if you use multinode training, make sure these envs are set
+export NNODES="${JOB_ARGS[0]}"
+export NPROC_PER_NODE="${JOB_ARGS[1]}"
+export GPUS_PER_NODE="${JOB_ARGS[1]}"
+export MASTER_ADDR="${JOB_ARGS[2]}"
+export MASTER_PORT="${JOB_ARGS[3]}"
+export NODE_RANK="${JOB_ARGS[4]}"
+
+export LLM_EVALUATOR_URL="URL"
+export LLM_EVALUATOR_API_KEY="API_KEY"
+export LLM_EVALUATOR_MODEL="Doubao-Seed-1.6"
+export LLM_EVALUATOR_MAX_SCORES=5
+export LLM_EVALUATOR_PAUC_ADD_PREV_TURNS=0
+export LLM_EVALUATOR_REPETITION_IN_ALL_SPANS=1
+export LLM_EVALUATOR_REPETITION_WEIGHT=2
+export LLM_EVALUATOR_OUTSPAN_WEIGHT=0.5
+export LLM_EVALUATOR_COMMON_PREFIX_WEIGHT=2
+
+export VERL_AUTO_PADDING=TRUE
+export RAY_IGNORE_UNHANDLED_ERRORS=1
+export TP_SIZE=2
+
+# --------------------
+# 1. start multi node ray cluster
+# --------------------
+
+export RAY_OBJECT_STORE_ALLOW_SLOW_STORAGE=1
+if [ "$NODE_RANK" -eq 0 ]; then
+    timestamp=$(date +"%Y%m%d-%H%M%S")
+    export TENSORBOARD_DIR=tensorboard_log/${timestamp}
+    mkdir $TENSORBOARD_DIR
+    
+    MAX_FRAMES=180
+    MAX_PROMPT_LENGTH=$((MAX_FRAMES*190))
+    MAX_RESP_FRAMES=90
+    MAX_RESP_LENGTH=$((MAX_RESP_FRAMES*190))
+
+    ray start --head --dashboard-host=0.0.0.0 --object-store-memory $((126*1024*1024*1024))
+    sleep 30s
+else
+    sleep 15s
+    ray start --address="$MASTER_ADDR:6379" --object-store-memory $((126*1024*1024*1024))
+fi
+
+ray status
+
+
+# --------------------
+# 2. start verl training
+# --------------------
+
+if [ "$NODE_RANK" -eq 0 ]; then
+    export SYSTEM_PROMPT="You are a helpful assistant. Your task is to answer questions based on continuously incoming video frames. Your responses should include information from the video since your last reply (if any). If the information in this segment of the video cannot answer the question, output \"NO REPLY\"."
+    mkdir ../nohup/proactive
+    ckpt_path=../train/ckpt/qwen2_5/SFT_CKPT
+
+    python -u -m verl.trainer.main_ppo \
+        algorithm.adv_estimator=grpo \
+        data.train_files=../data/live_whisperx-egoexo4d-egoexolearn0913-half_multi_half_single_question-1_sec_per_frame-20s_seg_as_example-0714-max_${MAX_FRAMES}_frames-train.parquet \
+        data.val_files=../data/live_whisperx-egoexo4d-egoexolearn0913-half_multi_half_single_question-1_sec_per_frame-20s_seg_as_example-0714-max_${MAX_FRAMES}_frames-val.parquet \
+        data.shuffle=False \
+        actor_rollout_ref.rollout.multi_turn.tool_config_path=./recipe/proactive/tool_config/live_whisperx-egoexo4d-egoexolearn0913-half_multi_half_single_question-20s_seg_as_example-1_sec_per_frame-max_${MAX_FRAMES}_frames.yaml \
+        data.train_batch_size=$((NNODES*NPROC_PER_NODE/TP_SIZE)) \
+        data.val_batch_size=$((NNODES*NPROC_PER_NODE/TP_SIZE)) \
+        data.max_prompt_length=$MAX_PROMPT_LENGTH \
+        data.filter_overlong_prompts=False \
+        data.return_raw_chat=True \
+        data.truncation='left' \
+        actor_rollout_ref.model.path=$ckpt_path \
+        actor_rollout_ref.actor.optim.lr=1e-6 \
+        actor_rollout_ref.model.use_remove_padding=True \
+        actor_rollout_ref.model.enable_gradient_checkpointing=True \
+        actor_rollout_ref.actor.ppo_mini_batch_size=$((NNODES*NPROC_PER_NODE/TP_SIZE)) \
+        actor_rollout_ref.actor.use_dynamic_bsz=False \
+        actor_rollout_ref.actor.ppo_micro_batch_size_per_gpu=1 \
+        actor_rollout_ref.actor.ppo_max_token_len_per_gpu=$((MAX_PROMPT_LENGTH+MAX_RESP_LENGTH)) \
+        actor_rollout_ref.actor.use_kl_loss=False \
+        actor_rollout_ref.actor.kl_loss_coef=0.005 \
+        actor_rollout_ref.actor.kl_loss_type=low_var_kl \
+        actor_rollout_ref.actor.entropy_coeff=0 \
+        actor_rollout_ref.actor.fsdp_config.param_offload=True \
+        actor_rollout_ref.actor.fsdp_config.optimizer_offload=True \
+        actor_rollout_ref.actor.ulysses_sequence_parallel_size=$TP_SIZE \
+        actor_rollout_ref.rollout.tensor_model_parallel_size=$TP_SIZE \
+        actor_rollout_ref.rollout.gpu_memory_utilization=0.25 \
+        actor_rollout_ref.rollout.n=4 \
+        actor_rollout_ref.rollout.prompt_length=$MAX_PROMPT_LENGTH \
+        actor_rollout_ref.rollout.response_length=$MAX_RESP_LENGTH \
+        +actor_rollout_ref.rollout.max_new_tokens_per_generation=100 \
+        actor_rollout_ref.rollout.temperature=1.2 \
+        actor_rollout_ref.rollout.top_k=10 \
+        actor_rollout_ref.rollout.do_sample=True \
+        actor_rollout_ref.rollout.name=sglang \
+        actor_rollout_ref.rollout.log_prob_micro_batch_size_per_gpu=1 \
+        actor_rollout_ref.rollout.multi_turn.enable=True \
+        actor_rollout_ref.rollout.multi_turn.max_turns=$((MAX_FRAMES+10)) \
+        actor_rollout_ref.rollout.engine_kwargs.sglang.attention_backend=flashinfer \
+        +actor_rollout_ref.rollout.multi_turn.is_online_video_conversation=True \
+        actor_rollout_ref.ref.fsdp_config.param_offload=True \
+        actor_rollout_ref.ref.log_prob_micro_batch_size_per_gpu=1 \
+        algorithm.use_kl_in_reward=False \
+        trainer.critic_warmup=0 \
+        trainer.logger=['console','tensorboard'] \
+        trainer.project_name='proactive' \
+        trainer.experiment_name="0814-egoexolearn0913-1_sec-outspan_penalty_0.5-common_prefix_penalty_2_rep_penalty_2-max_${MAX_FRAMES}_frames-no_freeze_vit" \
+        trainer.val_before_train=False \
+        trainer.validation_data_dir=../nohup/proactive-${timestamp} \
+        trainer.n_gpus_per_node=$NPROC_PER_NODE \
+        trainer.nnodes=$NNODES \
+        trainer.save_freq=30 \
+        trainer.test_freq=30 \
+        trainer.total_epochs=1 \
+        reward_model.launch_reward_fn_async=True \
+        custom_reward_function.path=recipe/proactive/reward_function.py \
+        custom_reward_function.name=pauc_reward_function \
+        > ../nohup/proactive/${timestamp}-egoexolearn0913-1_sec-outspan_penalty_0.5-common_prefix_penalty_2_rep_penalty_2-max_${MAX_FRAMES}_frames-no_freeze_vit.log 2>&1
+
+        return 0
+    else
+        sleep infinity
+    fi
diff --git a/recipe/proactive/run_scripts/train_grpo-half_multi_half_single_question-2_sec-20s_seg_as_example-max_90_frames.sh b/recipe/proactive/run_scripts/train_grpo-half_multi_half_single_question-2_sec-20s_seg_as_example-max_90_frames.sh
new file mode 100644
index 0000000..ee87ba8
--- /dev/null
+++ b/recipe/proactive/run_scripts/train_grpo-half_multi_half_single_question-2_sec-20s_seg_as_example-max_90_frames.sh
@@ -0,0 +1,121 @@
+# if you use multinode training, make sure these envs are set
+export NNODES="${JOB_ARGS[0]}"
+export GPUS_PER_NODE="${JOB_ARGS[1]}"
+export NPROC_PER_NODE="${JOB_ARGS[1]}"
+export MASTER_ADDR="${JOB_ARGS[2]}"
+export MASTER_PORT="${JOB_ARGS[3]}"
+export NODE_RANK="${JOB_ARGS[4]}"
+
+export LLM_EVALUATOR_URL="URL"
+export LLM_EVALUATOR_API_KEY="API_KEY"
+export LLM_EVALUATOR_MODEL="Doubao-Seed-1.6"
+export LLM_EVALUATOR_MAX_SCORES=5
+export LLM_EVALUATOR_PAUC_ADD_PREV_TURNS=0
+export LLM_EVALUATOR_REPETITION_IN_ALL_SPANS=1
+export LLM_EVALUATOR_REPETITION_WEIGHT=2
+export LLM_EVALUATOR_OUTSPAN_WEIGHT=0.5
+export LLM_EVALUATOR_COMMON_PREFIX_WEIGHT=2
+
+export VERL_AUTO_PADDING=TRUE
+export RAY_IGNORE_UNHANDLED_ERRORS=1
+export TP_SIZE=1
+
+# --------------------
+# 1. start multi node ray cluster
+# --------------------
+
+export RAY_OBJECT_STORE_ALLOW_SLOW_STORAGE=1
+if [ "$NODE_RANK" -eq 0 ]; then
+    timestamp=$(date +"%Y%m%d-%H%M%S")
+    export TENSORBOARD_DIR=tensorboard_log/${timestamp}
+    mkdir $TENSORBOARD_DIR
+    
+    MAX_FRAMES=90
+    MAX_PROMPT_LENGTH=$((MAX_FRAMES*190))
+    MAX_RESP_FRAMES=45
+    MAX_RESP_LENGTH=$((MAX_RESP_FRAMES*190))
+
+    ray start --head --dashboard-host=0.0.0.0 --object-store-memory $((126*1024*1024*1024))
+    sleep 30s
+else
+    sleep 15s
+    ray start --address="$MASTER_ADDR:6379" --object-store-memory $((126*1024*1024*1024))
+fi
+
+ray status
+
+
+# --------------------
+# 2. start verl training
+# --------------------
+
+if [ "$NODE_RANK" -eq 0 ]; then
+    export SYSTEM_PROMPT="You are a helpful assistant. Your task is to answer questions based on continuously incoming video frames. Your responses should include information from the video since your last reply (if any). If the information in this segment of the video cannot answer the question, output \"NO REPLY\"."
+    mkdir ../nohup/proactive
+    ckpt_path=../train/ckpt/qwen2_5/SFT_CKPT
+
+    python -u -m verl.trainer.main_ppo \
+        algorithm.adv_estimator=grpo \
+        data.train_files=../data/live_whisperx-egoexo4d-egoexolearn0913-half_multi_half_single_question-2_sec_per_frame-20s_seg_as_example-0714-max_${MAX_FRAMES}_frames-train.parquet \
+        data.val_files=../data/live_whisperx-egoexo4d-egoexolearn0913-half_multi_half_single_question-2_sec_per_frame-20s_seg_as_example-0714-max_${MAX_FRAMES}_frames-val.parquet \
+        data.shuffle=False \
+        actor_rollout_ref.rollout.multi_turn.tool_config_path=./recipe/proactive/tool_config/live_whisperx-egoexo4d-egoexolearn0913-half_multi_half_single_question-20s_seg_as_example-2_sec_per_frame-max_${MAX_FRAMES}_frames.yaml \
+        data.train_batch_size=$((NNODES*NPROC_PER_NODE/TP_SIZE)) \
+        data.val_batch_size=$((NNODES*NPROC_PER_NODE/TP_SIZE)) \
+        data.max_prompt_length=$MAX_PROMPT_LENGTH \
+        data.filter_overlong_prompts=False \
+        data.return_raw_chat=True \
+        data.truncation='left' \
+        actor_rollout_ref.model.path=$ckpt_path \
+        actor_rollout_ref.actor.optim.lr=1e-6 \
+        actor_rollout_ref.model.use_remove_padding=True \
+        actor_rollout_ref.model.enable_gradient_checkpointing=True \
+        actor_rollout_ref.actor.ppo_mini_batch_size=8 \
+        actor_rollout_ref.actor.use_dynamic_bsz=False \
+        actor_rollout_ref.actor.ppo_micro_batch_size_per_gpu=1 \
+        actor_rollout_ref.actor.ppo_max_token_len_per_gpu=$((MAX_PROMPT_LENGTH+MAX_RESP_LENGTH)) \
+        actor_rollout_ref.actor.use_kl_loss=False \
+        actor_rollout_ref.actor.kl_loss_coef=0.005 \
+        actor_rollout_ref.actor.kl_loss_type=low_var_kl \
+        actor_rollout_ref.actor.entropy_coeff=0 \
+        actor_rollout_ref.actor.fsdp_config.param_offload=True \
+        actor_rollout_ref.actor.fsdp_config.optimizer_offload=True \
+        actor_rollout_ref.actor.ulysses_sequence_parallel_size=$TP_SIZE \
+        actor_rollout_ref.rollout.tensor_model_parallel_size=$TP_SIZE \
+        actor_rollout_ref.rollout.gpu_memory_utilization=0.25 \
+        actor_rollout_ref.rollout.n=4 \
+        actor_rollout_ref.rollout.prompt_length=$MAX_PROMPT_LENGTH \
+        actor_rollout_ref.rollout.response_length=$MAX_RESP_LENGTH \
+        +actor_rollout_ref.rollout.max_new_tokens_per_generation=100 \
+        actor_rollout_ref.rollout.temperature=1.2 \
+        actor_rollout_ref.rollout.top_k=10 \
+        actor_rollout_ref.rollout.do_sample=True \
+        actor_rollout_ref.rollout.name=sglang \
+        actor_rollout_ref.rollout.log_prob_micro_batch_size_per_gpu=1 \
+        actor_rollout_ref.rollout.multi_turn.enable=True \
+        actor_rollout_ref.rollout.multi_turn.max_turns=$((MAX_FRAMES+10)) \
+        actor_rollout_ref.rollout.engine_kwargs.sglang.attention_backend=flashinfer \
+        +actor_rollout_ref.rollout.multi_turn.is_online_video_conversation=True \
+        actor_rollout_ref.ref.fsdp_config.param_offload=True \
+        actor_rollout_ref.ref.log_prob_micro_batch_size_per_gpu=1 \
+        algorithm.use_kl_in_reward=False \
+        trainer.critic_warmup=0 \
+        trainer.logger=['console','tensorboard'] \
+        trainer.project_name='proactive' \
+        trainer.experiment_name="0814-egoexolearn0913-2_sec-outspan_penalty_0.5-common_prefix_penalty_2_rep_penalty_2-max_${MAX_FRAMES}_frames" \
+        trainer.val_before_train=False \
+        trainer.validation_data_dir=../nohup/proactive-${timestamp} \
+        trainer.n_gpus_per_node=$NPROC_PER_NODE \
+        trainer.nnodes=$NNODES \
+        trainer.save_freq=15 \
+        trainer.test_freq=30 \
+        trainer.total_epochs=1 \
+        reward_model.launch_reward_fn_async=True \
+        custom_reward_function.path=recipe/proactive/reward_function.py \
+        custom_reward_function.name=pauc_reward_function \
+        > ../nohup/proactive/${timestamp}-egoexolearn0913-2_sec-outspan_penalty_0.5-common_prefix_penalty_2_rep_penalty_2-max_${MAX_FRAMES}_frames.log 2>&1
+
+        return 0
+    else
+        sleep infinity
+    fi
diff --git a/recipe/proactive/tool_config/live_whisperx-egoexo4d-egoexolearn0913-half_multi_half_single_question-20s_seg_as_example-1_sec_per_frame-max_180_frames.yaml b/recipe/proactive/tool_config/live_whisperx-egoexo4d-egoexolearn0913-half_multi_half_single_question-20s_seg_as_example-1_sec_per_frame-max_180_frames.yaml
new file mode 100644
index 0000000..bcac903
--- /dev/null
+++ b/recipe/proactive/tool_config/live_whisperx-egoexo4d-egoexolearn0913-half_multi_half_single_question-20s_seg_as_example-1_sec_per_frame-max_180_frames.yaml
@@ -0,0 +1,20 @@
+tools:
+  - class_name: "verl.tools.online_video_provider.OnlineVideoProvider"
+    config: {
+      "annotation_paths": [
+        "../data/live_whisperx-egoexo4d-egoexolearn0913-half_multi_half_single_question-1_sec_per_frame-20s_seg_as_example-0714-max_180_frames-user_turns.json",
+      ]
+    }
+    tool_schema:
+      type: "function"
+      function:
+        name: "online_video_provider"
+        description: "A tool to fetch the next user turn in video-text online conversation"
+        parameters:
+          type: "object"
+          properties:
+            question_id:
+              type: "string"
+            turn_id:
+              type: "int"
+          required: ["question_id", "turn_id"]
diff --git a/recipe/proactive/tool_config/live_whisperx-egoexo4d-egoexolearn0913-half_multi_half_single_question-20s_seg_as_example-2_sec_per_frame-max_90_frames.yaml b/recipe/proactive/tool_config/live_whisperx-egoexo4d-egoexolearn0913-half_multi_half_single_question-20s_seg_as_example-2_sec_per_frame-max_90_frames.yaml
new file mode 100644
index 0000000..46255ef
--- /dev/null
+++ b/recipe/proactive/tool_config/live_whisperx-egoexo4d-egoexolearn0913-half_multi_half_single_question-20s_seg_as_example-2_sec_per_frame-max_90_frames.yaml
@@ -0,0 +1,20 @@
+tools:
+  - class_name: "verl.tools.online_video_provider.OnlineVideoProvider"
+    config: {
+      "annotation_paths": [
+        "../data/live_whisperx-egoexo4d-egoexolearn0913-half_multi_half_single_question-2_sec_per_frame-20s_seg_as_example-0714-max_90_frames-user_turns.json",
+      ]
+    }
+    tool_schema:
+      type: "function"
+      function:
+        name: "online_video_provider"
+        description: "A tool to fetch the next user turn in video-text online conversation"
+        parameters:
+          type: "object"
+          properties:
+            question_id:
+              type: "string"
+            turn_id:
+              type: "int"
+          required: ["question_id", "turn_id"]
diff --git a/verl/protocol.py b/verl/protocol.py
index 67d1bf0..1d78c4e 100644
--- a/verl/protocol.py
+++ b/verl/protocol.py
@@ -18,6 +18,7 @@ We can subclass Protocol to define more detailed batch info with specific keys
 
 import contextlib
 import copy
+import math
 import logging
 import os
 import pickle
@@ -644,9 +645,17 @@ class DataProto:
         """
         if padding_size == 0:
             return
-        padding_candidate = self.select_idxs([0 if padding_candidate == "first" else len(self) - 1])
-        padding_part = padding_candidate.repeat(padding_size)
-        padded_dp = DataProto.concat([self, padding_part])
+        if padding_candidate == 'interleave':       # when device_mesh.size() == [dp, tp], this can ensure the data is same in each dp group
+            repeat_times = math.ceil((padding_size + len(self)) / len(self))
+            data_idx_list = list()
+            for idx in range(len(self)):
+                data_idx_list += [idx] * repeat_times
+            data_idx_list = data_idx_list[:len(self) + padding_size]
+            padded_dp = self.select_idxs(data_idx_list)
+        else:
+            padding_candidate = self.select_idxs([0 if padding_candidate == "first" else len(self) - 1])
+            padding_part = padding_candidate.repeat(padding_size)
+            padded_dp = DataProto.concat([self, padding_part])
         self.batch = padded_dp.batch
         self.non_tensor_batch = padded_dp.non_tensor_batch
 
diff --git a/verl/single_controller/base/decorator.py b/verl/single_controller/base/decorator.py
index 4867420..55da61f 100644
--- a/verl/single_controller/base/decorator.py
+++ b/verl/single_controller/base/decorator.py
@@ -12,6 +12,7 @@
 # See the License for the specific language governing permissions and
 # limitations under the License.
 
+import os
 import inspect
 from functools import wraps
 from types import FunctionType
@@ -47,6 +48,7 @@ def init_predefined_dispatch_mode():
     Dispatch.register("MEGATRON_PP_AS_DP_PROTO")
     Dispatch.register("DP_COMPUTE")
     Dispatch.register("DP_COMPUTE_PROTO")
+    Dispatch.register("DP_COMPUTE_PROTO_NO_TP")
     Dispatch.register("DP_COMPUTE_PROTO_WITH_FUNC")
     Dispatch.register("DP_COMPUTE_METRIC")
     # This is a special dispatch mode for vllm ExternalRayDistributedExecutor
@@ -90,7 +92,7 @@ def _split_args_kwargs_data_proto(chunks, *args, **kwargs):
     return splitted_args, splitted_kwargs
 
 
-def _split_args_kwargs_data_proto_with_auto_padding(chunks, *args, **kwargs):
+def _split_args_kwargs_data_proto_with_auto_padding(chunks, tp_size, *args, **kwargs):
     from verl.protocol import DataProto, DataProtoFuture
 
     splitted_args = []
@@ -105,11 +107,14 @@ def _split_args_kwargs_data_proto_with_auto_padding(chunks, *args, **kwargs):
             if data_proto_len is None:
                 data_proto_len = len(arg)
                 padding_size = (chunks - (data_proto_len % chunks)) if (data_proto_len % chunks > 0) else 0
-                splitted_kwargs[_padding_size_key] = padding_size
+                dp_chunks = chunks // tp_size
+                dp_padding_size = (dp_chunks - (data_proto_len % dp_chunks)) if (data_proto_len % dp_chunks > 0) else 0
+                splitted_kwargs[_padding_size_key] = dp_padding_size        # splitted_kwargs[_padding_size_key] is used to truncate rollout output in collect_fn. But we do not always need to truncate
             else:
                 assert data_proto_len == len(arg), f"expecting all arg share same length of {data_proto_len}, but got {len(arg)}"
                 data_proto_len = len(arg)
-            arg.padding(padding_size=padding_size)
+                dp_padding_size = 0
+            arg.padding(padding_size=padding_size, padding_candidate='last' if dp_padding_size else 'interleave')
 
         splitted_args.append(arg.chunk(chunks=chunks))
 
@@ -120,10 +125,14 @@ def _split_args_kwargs_data_proto_with_auto_padding(chunks, *args, **kwargs):
             if data_proto_len is None:
                 data_proto_len = len(val)
                 padding_size = chunks - (data_proto_len % chunks)
-                splitted_kwargs[_padding_size_key] = padding_size
+                dp_chunks = chunks // tp_size
+                dp_padding_size = (dp_chunks - (data_proto_len % dp_chunks)) if (data_proto_len % dp_chunks > 0) else 0
+                splitted_kwargs[_padding_size_key] = dp_padding_size
             else:
                 assert data_proto_len == len(val), f"expecting all arg share same length of {data_proto_len}, but got {len(val)}"
                 data_proto_len = len(val)
+                dp_padding_size = 0
+            val.padding(padding_size=padding_size, padding_candidate='last' if dp_padding_size else 'interleave')
         splitted_kwargs[key] = val.chunk(chunks=chunks)
 
     return splitted_args, splitted_kwargs
@@ -366,6 +375,20 @@ def collect_dp_compute(worker_group, output):
     return output
 
 
+def dispatch_dp_compute_data_proto_no_tp(worker_group, *args, **kwargs):
+    from verl.single_controller.base.worker_group import WorkerGroup
+
+    assert isinstance(worker_group, WorkerGroup)
+    # Note: enable auto padding for dp compute DatapProto
+    splitted_args, splitted_kwargs = _split_args_kwargs_data_proto_with_auto_padding(
+        worker_group.world_size,
+        int(os.environ.get("TP_SIZE", 1)),
+        *args,
+        **kwargs,
+    )
+    return splitted_args, splitted_kwargs
+
+
 def dispatch_dp_compute_data_proto(worker_group, *args, **kwargs):
     from verl.single_controller.base.worker_group import WorkerGroup
 
@@ -373,6 +396,7 @@ def dispatch_dp_compute_data_proto(worker_group, *args, **kwargs):
     # Note: enable auto padding for dp compute DatapProto
     splitted_args, splitted_kwargs = _split_args_kwargs_data_proto_with_auto_padding(
         worker_group.world_size,
+        1,
         *args,
         **kwargs,
     )
@@ -399,7 +423,8 @@ def collect_dp_compute_data_proto(worker_group, output):
         assert isinstance(o, (DataProto, ray.ObjectRef)), f"expecting {o} to be DataProto, but got {type(o)}"
 
     output = collect_dp_compute(worker_group, output)
-    return _concat_data_proto_or_future(output)
+    output = _concat_data_proto_or_future(output)
+    return output
 
 
 # Global registry for dispatch mode.
@@ -434,6 +459,10 @@ DISPATCH_MODE_FN_REGISTRY = {
         "dispatch_fn": dispatch_dp_compute_data_proto,
         "collect_fn": collect_dp_compute_data_proto,
     },
+    Dispatch.DP_COMPUTE_PROTO_NO_TP: {
+        "dispatch_fn": dispatch_dp_compute_data_proto_no_tp,
+        "collect_fn": collect_dp_compute_data_proto,
+    },
     Dispatch.DP_COMPUTE_PROTO_WITH_FUNC: {
         "dispatch_fn": dispatch_dp_compute_data_proto_with_func,
         "collect_fn": collect_dp_compute_data_proto,
diff --git a/verl/single_controller/base/worker.py b/verl/single_controller/base/worker.py
index 6dae88b..793aef5 100644
--- a/verl/single_controller/base/worker.py
+++ b/verl/single_controller/base/worker.py
@@ -295,3 +295,7 @@ class Worker(WorkerHelper):
         """
         result = func(*args, **kwargs)
         return result
+
+    def print_rank0(self, *args, **kwargs):
+        if getattr(self, "rank", 0) ==  0:
+           print(*args, **kwargs)
diff --git a/verl/tools/online_video_provider.py b/verl/tools/online_video_provider.py
new file mode 100644
index 0000000..fab34ef
--- /dev/null
+++ b/verl/tools/online_video_provider.py
@@ -0,0 +1,63 @@
+
+import logging
+import json
+import os
+from typing import Any, Tuple
+
+from verl.tools.base_tool import BaseTool
+from .schemas import OpenAIFunctionToolSchema
+from sglang.srt.function_call_parser import ToolCallItem, StreamingParseResult, FunctionCallParser
+
+logger = logging.getLogger(__file__)
+logger.setLevel(os.getenv("VERL_LOGGING_LEVEL", "WARN"))
+
+
+class OnlineVideoProvider(BaseTool):
+    def __init__(self, config: dict, tool_schema: OpenAIFunctionToolSchema):
+        super().__init__(config, tool_schema)
+        log_msg = f"Init {self.__class__.__name__} with config: {config}"
+        logger.info(log_msg)
+        self.annotation = dict()
+        for path in config.get("annotation_paths", list()):
+            self.annotation.update(json.load(open(path)))
+
+    async def execute(self, instance_id: str, parameters: dict[str, Any], **kwargs) -> Tuple[str, float, dict]:
+        # response, reward, metrics
+        question_id = kwargs.get("question_id", None)
+        turn_id = parameters.get("turn_id", None)
+        if turn_id is None:
+            logger.error(f"{parameters=} should contain turn_id")
+            return "Invalid parameters", 0, None
+        if question_id is None:
+            logger.error(f"{kwargs=} should contain question_id")
+            return "Invalid parameters", 0, None
+        
+        user_turns = self.annotation[question_id]
+        if turn_id >= len(user_turns):
+            return "", 0, -1
+
+        next_user_turn = json.dumps(user_turns[turn_id]['content'])
+        return next_user_turn, 0, 0
+
+
+class ProvideOnlineVideoFunctionCallParser(FunctionCallParser):
+    """
+    each turn from the assistant should trigger a tool call.
+    And the tool call should always call OnlineVideoProvider
+    """
+    def __init__(self, tools):
+        self.tools = tools
+
+    def has_tool_call(self, content):
+        return True
+
+    def parse_non_stream(self, content, messages):
+        num_user_turns_in_messages = len([turn for turn in messages if turn.role == "user"])
+        tool_call_results = [
+            ToolCallItem(
+                tool_index=0,       # the 0th tool is OnlineVideoProvider?
+                name="online_video_provider",
+                parameters=json.dumps({"turn_id": num_user_turns_in_messages})
+            )
+        ]
+        return content, tool_call_results
diff --git a/verl/trainer/ppo/ray_trainer.py b/verl/trainer/ppo/ray_trainer.py
index 2238a4d..0d5f26e 100644
--- a/verl/trainer/ppo/ray_trainer.py
+++ b/verl/trainer/ppo/ray_trainer.py
@@ -95,7 +95,7 @@ class ResourcePoolManager:
             resource_pool = RayResourcePool(process_on_nodes=process_on_nodes, use_gpu=True, max_colocate_count=1, name_prefix=resource_pool_name)
             self.resource_pool_dict[resource_pool_name] = resource_pool
 
-        self._check_resource_available()
+        # self._check_resource_available()
 
     def get_resource_pool(self, role: Role) -> RayResourcePool:
         """Get the resource pool of the worker_cls"""
@@ -1107,6 +1107,10 @@ class RayPPOTrainer:
                                 dump_path=rollout_data_dir,
                             )
 
+                    if self.config.trainer.save_freq > 0 and (is_last_step or self.global_steps % self.config.trainer.save_freq == 0):
+                        with _timer("save_checkpoint", timing_raw):
+                            self._save_checkpoint()
+
                     # validate
                     if self.val_reward_fn is not None and self.config.trainer.test_freq > 0 and (is_last_step or self.global_steps % self.config.trainer.test_freq == 0):
                         with _timer("testing", timing_raw):
@@ -1115,10 +1119,6 @@ class RayPPOTrainer:
                                 last_val_metrics = val_metrics
                         metrics.update(val_metrics)
 
-                    if self.config.trainer.save_freq > 0 and (is_last_step or self.global_steps % self.config.trainer.save_freq == 0):
-                        with _timer("save_checkpoint", timing_raw):
-                            self._save_checkpoint()
-
                 # training metrics
                 metrics.update(
                     {
diff --git a/verl/trainer/ppo/reward.py b/verl/trainer/ppo/reward.py
index 323be6c..0a0b89b 100644
--- a/verl/trainer/ppo/reward.py
+++ b/verl/trainer/ppo/reward.py
@@ -51,9 +51,9 @@ def get_custom_reward_fn(config):
 
     reward_kwargs = dict(reward_fn_config.get("reward_kwargs", {}))
 
-    def wrapped_fn(*args, **kwargs):
-        return raw_fn(*args, **kwargs, **reward_kwargs)
-
+    # def wrapped_fn(*args, **kwargs):
+    #     return raw_fn(*args, **kwargs, **reward_kwargs)
+    wrapped_fn = partial(raw_fn, **reward_kwargs)           # make it picklable
     return wrapped_fn
 
 
diff --git a/verl/utils/dataset/rl_dataset.py b/verl/utils/dataset/rl_dataset.py
index 327764d..099e8a9 100644
--- a/verl/utils/dataset/rl_dataset.py
+++ b/verl/utils/dataset/rl_dataset.py
@@ -161,6 +161,8 @@ class RLHFDataset(Dataset):
 
     def _build_messages(self, example: dict):
         messages: list = example.pop(self.prompt_key)
+        images_list = example.get(self.image_key, list())
+        videos_list = example.get(self.video_key, list())
 
         if self.image_key in example or self.video_key in example:
             for message in messages:
@@ -168,9 +170,11 @@ class RLHFDataset(Dataset):
                 content_list = []
                 for segment in re.split("(<image>|<video>)", content):
                     if segment == "<image>":
-                        content_list.append({"type": "image"})
+                        content_list.append(images_list.pop(0))
                     elif segment == "<video>":
-                        content_list.append({"type": "video"})
+                        content_list.append(videos_list.pop(0))
+                    elif not len(segment):
+                        continue
                     else:
                         content_list.append({"type": "text", "text": segment})
 
@@ -187,35 +191,39 @@ class RLHFDataset(Dataset):
         model_inputs = {}
 
         if self.processor is not None:
-            from verl.utils.dataset.vision_utils import process_image, process_video
+            # from verl.utils.dataset.vision_utils import process_image, process_video
+            from qwen_vl_utils import process_vision_info
 
             raw_prompt = self.processor.apply_chat_template(messages, add_generation_prompt=True, tokenize=False)
-            multi_modal_data = {}
+            images, videos = process_vision_info(messages)
 
-            images = None
-            if self.image_key in row_dict:
-                images = [process_image(image) for image in row_dict.pop(self.image_key)]
-                multi_modal_data["image"] = images
+            # images = None
+            # if self.image_key in row_dict:
+            #     images = [process_image(image) for image in row_dict.pop(self.image_key)]
+            #     multi_modal_data["image"] = images
 
-            videos = None
-            if self.video_key in row_dict:
-                videos = [process_video(video) for video in row_dict.pop(self.video_key)]
-                multi_modal_data["video"] = [video.numpy() for video in videos]
+            # videos = None
+            # if self.video_key in row_dict:
+            #     videos = [process_video(video) for video in row_dict.pop(self.video_key)]
+            #     multi_modal_data["video"] = [np.array(video) for video in videos]
 
             model_inputs = self.processor(text=[raw_prompt], images=images, videos=videos, return_tensors="pt")
-
+            # Though the values of `input_ids` and `attention_mask` are not used in rollout, they are still required when calculating log probs.
+            # so we still need to preprocess them properly.
             input_ids = model_inputs.pop("input_ids")
             attention_mask = model_inputs.pop("attention_mask")
 
-            if "second_per_grid_ts" in model_inputs:
-                model_inputs.pop("second_per_grid_ts")
+            # multi_modal_data will be set in rollout stage, as we need to add new frames in the interaction.
+            # therefore, multi_modal_inputs is not preprocessed here.
+            # if "second_per_grid_ts" in model_inputs:
+            #     model_inputs.pop("second_per_grid_ts")
 
             # There's a trap here, multi_modal_inputs has to be a dict, not BatchFeature
-            row_dict["multi_modal_data"] = multi_modal_data
-            row_dict["multi_modal_inputs"] = dict(model_inputs)
+            # row_dict["multi_modal_data"] = multi_modal_data
+            # row_dict["multi_modal_inputs"] = dict(model_inputs)
 
             # second_per_grid_ts isn't used for training, just for mrope
-            row_dict["multi_modal_inputs"].pop("second_per_grid_ts", None)
+            # row_dict["multi_modal_inputs"].pop("second_per_grid_ts", None)
 
         else:
             raw_prompt = self.tokenizer.apply_chat_template(messages, add_generation_prompt=True, tokenize=False)
diff --git a/verl/utils/dataset/vision_utils.py b/verl/utils/dataset/vision_utils.py
index 832abde..fc030ef 100644
--- a/verl/utils/dataset/vision_utils.py
+++ b/verl/utils/dataset/vision_utils.py
@@ -55,7 +55,9 @@ eg.
     "fps": 2,
     "min_frames": 1,
     "max_frames": 32
-}
+}.
+
+Your input is: {}
 """
 
 
@@ -72,7 +74,7 @@ def process_video(
     """
 
     if not isinstance(video, dict) or "video" not in video:
-        raise NotImplementedError(VIDEO_FORMAT_HELP)
+        raise NotImplementedError(VIDEO_FORMAT_HELP.format(video))
     assert nframes is None or fps is None, "Can't use both `nframes` or `fps`"
 
     # Shallow copy... since we might want to add some keys
diff --git a/verl/workers/fsdp_workers.py b/verl/workers/fsdp_workers.py
index 198316a..2731549 100644
--- a/verl/workers/fsdp_workers.py
+++ b/verl/workers/fsdp_workers.py
@@ -222,6 +222,7 @@ class ActorRolloutRefWorker(Worker):
         # NOTE(fix me): tie_word_embedding causes meta_tensor init to hang
         init_context = get_init_weight_context_manager(use_meta_tensor=not actor_model_config.tie_word_embeddings, mesh=self.device_mesh)
 
+        use_orig_params = False
         with init_context(), warnings.catch_warnings():
             warnings.simplefilter("ignore")
             if type(actor_model_config) in AutoModelForVision2Seq._model_mapping.keys():
@@ -264,6 +265,19 @@ class ActorRolloutRefWorker(Worker):
                 # Convert config to regular Python types before creating PEFT model
                 lora_config = {"task_type": TaskType.CAUSAL_LM, "r": self.config.model.lora_rank, "lora_alpha": self.config.model.lora_alpha, "target_modules": convert_to_regular_types(self.config.model.target_modules), "bias": "none"}
                 actor_module = get_peft_model(actor_module, LoraConfig(**lora_config))
+
+            if optim_config.get('freeze_vision_tower', False):
+                if hasattr(actor_module, "model") and hasattr(actor_module.model, "visual"):  # transformers >= 4.52.0
+                    actor_module.model.visual.requires_grad_(False)
+                    use_orig_params = True
+                    self.print_rank0("Vision tower model.model.visual is set to not trainable.")
+                elif hasattr(actor_module, "visual"):  # transformers < 4.52.0
+                    actor_module.visual.requires_grad_(False)
+                    use_orig_params = True
+                    self.print_rank0("Vision tower model.visual is set to not trainable.")
+                else:
+                    self.print_rank0("Setting optim_config.freeze_vision_tower=True, No vision tower found.")
+
         torch.distributed.barrier()
 
         if self.rank == 0:
@@ -306,7 +320,7 @@ class ActorRolloutRefWorker(Worker):
                 actor_module,
                 cpu_offload=cpu_offload,
                 param_init_fn=init_fn,
-                use_orig_params=False,
+                use_orig_params=use_orig_params,
                 auto_wrap_policy=auto_wrap_policy,
                 device_id=get_device_id(),
                 sharding_strategy=sharding_strategy,  # zero3
@@ -453,6 +467,7 @@ class ActorRolloutRefWorker(Worker):
                 actor_module=local_path,
                 config=self.config.rollout,
                 tokenizer=self.tokenizer,
+                processor=self.processor,
                 model_hf_config=self.actor_model_config,
                 trust_remote_code=trust_remote_code,
             )
@@ -467,6 +482,7 @@ class ActorRolloutRefWorker(Worker):
                 full_params="hf" in self.config.rollout.load_format,
                 device_mesh=rollout_device_mesh,
                 offload_param=self._is_offload_param,
+                skip_gather_data_in_preprocess=self.config.rollout.multi_turn.enable,      # if multi_turn, sglang use messages not tokenized input_ids as input. so no need to chunk the tensordict input.
             )
             log_gpu_memory_usage("After building sharding manager", logger=logger)
 
@@ -542,7 +558,11 @@ class ActorRolloutRefWorker(Worker):
             self.rollout, self.rollout_sharding_manager = self._build_rollout(trust_remote_code=self.config.model.get("trust_remote_code", False))
 
         if self._is_ref:
-            local_path = copy_to_local(self.config.model.path, use_shm=use_shm)
+            ref_model_path = self.config.model.get("ref_path", self.config.model.path)
+            actor_model_path = self.config.model.path
+            if ref_model_path != actor_model_path:
+                logger.warn(f"Using different model for actor and ref: {actor_model_path=}, {ref_model_path=}")
+            local_path = copy_to_local(ref_model_path, use_shm=use_shm)
             self.ref_module_fsdp = self._build_model_optimizer(
                 model_path=local_path,
                 fsdp_config=self.config.ref.fsdp_config,
@@ -626,8 +646,9 @@ class ActorRolloutRefWorker(Worker):
 
         return output
 
-    @register(dispatch_mode=Dispatch.DP_COMPUTE_PROTO)
+    @register(dispatch_mode=Dispatch.DP_COMPUTE_PROTO_NO_TP)
     def generate_sequences(self, prompts: DataProto):
+        get_torch_device().empty_cache()
         # Support all hardwares
         prompts = prompts.to(get_device_id())
 
diff --git a/verl/workers/reward_manager/naive.py b/verl/workers/reward_manager/naive.py
index ec9709b..c12a82a 100644
--- a/verl/workers/reward_manager/naive.py
+++ b/verl/workers/reward_manager/naive.py
@@ -20,6 +20,8 @@ from verl import DataProto
 from verl.utils.reward_score import default_compute_score
 from verl.workers.reward_manager import register
 
+import multiprocessing as mp
+import os
 
 @register("naive")
 class NaiveRewardManager:
@@ -39,6 +41,7 @@ class NaiveRewardManager:
         self.num_examine = num_examine  # the number of batches of decoded responses to print to the console
         self.compute_score = compute_score or default_compute_score
         self.reward_fn_key = reward_fn_key  # Store the key for accessing the data source
+        self.num_processes = int(os.environ.get("LLM_EVALUATOR_NUM_PROCESSES", 20))     # num processes for getting reward in parallel
 
     def __call__(self, data: DataProto, return_dict=False):
         """We will expand this function gradually based on the available datasets"""
@@ -53,15 +56,19 @@ class NaiveRewardManager:
         reward_tensor = torch.zeros_like(data.batch["responses"], dtype=torch.float32)
         reward_extra_info = defaultdict(list)
 
-        already_print_data_sources = {}
+        prompts_ids = []
+        responses_ids = []
+        prompt_lengths = []
+        response_lengths = []
+        data_sources = []
+        ground_truths = []
+        extra_infos = []
 
         for i in range(len(data)):
-            data_item = data[i]  # DataProtoItem
+            data_item = data[i]
 
             prompt_ids = data_item.batch["prompts"]
-
             prompt_length = prompt_ids.shape[-1]
-
             valid_prompt_length = data_item.batch["attention_mask"][:prompt_length].sum()
             valid_prompt_ids = prompt_ids[-valid_prompt_length:]
 
@@ -69,20 +76,26 @@ class NaiveRewardManager:
             valid_response_length = data_item.batch["attention_mask"][prompt_length:].sum()
             valid_response_ids = response_ids[:valid_response_length]
 
-            # decode
-            prompt_str = self.tokenizer.decode(valid_prompt_ids, skip_special_tokens=True)
-            response_str = self.tokenizer.decode(valid_response_ids, skip_special_tokens=True)
+            prompts_ids.append(valid_prompt_ids)
+            responses_ids.append(valid_response_ids)
+            prompt_lengths.append(valid_prompt_length)
+            response_lengths.append(valid_response_length)
+            data_sources.append(data_item.non_tensor_batch[self.reward_fn_key])
+            ground_truths.append(data_item.non_tensor_batch["reward_model"]["ground_truth"])
+            extra_infos.append(data_item.non_tensor_batch.get("extra_info", None))
 
-            ground_truth = data_item.non_tensor_batch["reward_model"]["ground_truth"]
-            data_source = data_item.non_tensor_batch[self.reward_fn_key]
-            extra_info = data_item.non_tensor_batch.get("extra_info", None)
+        prompt_strs = [self.tokenizer.decode(ids, skip_special_tokens=True) for ids in prompts_ids]
+        response_strs = [self.tokenizer.decode(ids, skip_special_tokens=True) for ids in responses_ids]
 
-            score = self.compute_score(
-                data_source=data_source,
-                solution_str=response_str,
-                ground_truth=ground_truth,
-                extra_info=extra_info,
-            )
+        print(f"geting reward with {self.num_processes} processes in parallel...")
+        with mp.Pool(processes=self.num_processes) as pool:
+            args = [(data_sources[i], response_strs[i], ground_truths[i], extra_infos[i])
+                    for i in range(len(data))]
+            scores = pool.starmap(self.compute_score, args)
+
+        already_print_data_sources = {}
+        for i in range(len(data)):
+            score = scores[i]
 
             if isinstance(score, dict):
                 reward = score["score"]
@@ -92,16 +105,17 @@ class NaiveRewardManager:
             else:
                 reward = score
 
-            reward_tensor[i, valid_response_length - 1] = reward
+            reward_tensor[i, response_lengths[i] - 1] = reward
 
+            data_source = data_sources[i]
             if data_source not in already_print_data_sources:
                 already_print_data_sources[data_source] = 0
 
             if already_print_data_sources[data_source] < self.num_examine:
                 already_print_data_sources[data_source] += 1
-                print("[prompt]", prompt_str)
-                print("[response]", response_str)
-                print("[ground_truth]", ground_truth)
+                print("[prompt]", prompt_strs[i])
+                print("[response]", response_strs[i])
+                print("[ground_truth]", ground_truths[i])
                 if isinstance(score, dict):
                     for key, value in score.items():
                         print(f"[{key}]", value)
diff --git a/verl/workers/rollout/schemas.py b/verl/workers/rollout/schemas.py
index b259d04..a899599 100644
--- a/verl/workers/rollout/schemas.py
+++ b/verl/workers/rollout/schemas.py
@@ -15,11 +15,13 @@
 import logging
 import os
 from enum import Enum
-from typing import Any, Dict, List, Optional
+import json
+from typing import Any, Dict, List, Optional, Union
+from PIL.Image import Image
 
 import torch
-from pydantic import BaseModel, model_validator
-from transformers import PreTrainedTokenizer
+from pydantic import BaseModel, model_validator, ConfigDict
+from transformers import PreTrainedTokenizer, ProcessorMixin
 
 from verl.tools.schemas import OpenAIFunctionToolCall, OpenAIFunctionToolSchema
 from verl.utils.model import compute_position_id_with_mask
@@ -27,8 +29,9 @@ from verl.utils.model import compute_position_id_with_mask
 logger = logging.getLogger(__file__)
 logger.setLevel(os.getenv("VERL_LOGGING_LEVEL", "WARN"))
 
-BASE_CHAT_HISTORY = [{"role": "system", "content": "You are a helpful assistant."}, {"role": "user", "content": "I am a user."}]
-
+SYSTEM_PROMPT = os.environ.get("SYSTEM_PROMPT", "You are a helpful assistant.")
+BASE_CHAT_HISTORY = [{"role": "system", "content": SYSTEM_PROMPT}, {"role": "user", "content": "I am a user."}]
+logger.warn(f"using system prompt: {SYSTEM_PROMPT}. This system prompt will be added to the first turn of the conversation if the first turn is not a system prompt.")
 
 class FinishReasonTypeEnum(str, Enum):
     """The enum for finish reason type."""
@@ -51,7 +54,7 @@ class FinishReasonTypeEnum(str, Enum):
 
 class Message(BaseModel):
     role: str
-    content: str
+    content: Union[str, Dict, List]
     tool_calls: Optional[List[OpenAIFunctionToolCall]] = None
 
 
@@ -67,6 +70,7 @@ class AsyncRolloutRequestStateEnum(str, Enum):
 
 class AsyncRolloutRequest(BaseModel):
     """The data model for async rollout."""
+    model_config = ConfigDict(arbitrary_types_allowed=True)
 
     batch_data_id: int = 0
     rollout_offset: int = 0
@@ -93,6 +97,10 @@ class AsyncRolloutRequest(BaseModel):
     max_model_len: int = 32768
     metrics: Dict[str, List[Any]] = {}
 
+    pixel_values: Optional[torch.Tensor] = None
+    image_grid_thw: Optional[torch.Tensor] = None
+    images: List[Union[str, Image]] = list()
+
     use_inference_chat_template: bool
     enable_tokenization_sanity_check: bool
     generation_prompt_ids: List[int]
@@ -108,14 +116,57 @@ class AsyncRolloutRequest(BaseModel):
             raise ValueError("max_prompt_len is required for AsyncRolloutRequest initialization")
         if not (tokenizer := values.pop("tokenizer", None)):
             raise ValueError("tokenizer is required for AsyncRolloutRequest initialization")
+        processor = values.pop("processor", None)
+        is_qwen2_5_vl = processor is not None and 'Qwen2_5_VL'in processor.__class__.__name__
 
-        values["messages"] = [Message.model_validate(msg) for msg in messages]
+        # add system prom
+        if not len(messages) or messages[0]['role'] != "system":
+            messages.insert(0, {"role": "system", "content": SYSTEM_PROMPT})
+        else:
+            # if messages[0]['content'] != SYSTEM_PROMPT:
+            #     logger.warn(f"System prompt {messages[0]['content']} is not the same as the global system prompt set in workers.rollout.schemas: {SYSTEM_PROMPT=}")
+            pass        # if the data has a system prompt, use the system prompt in data.
 
+        values["messages"] = [Message.model_validate(msg) for msg in messages]
         tools = [tool.model_dump() for tool in tool_schemas] if (tool_schemas := values.get("tool_schemas", [])) else None
-        tokens_without_prompt = tokenizer.apply_chat_template(messages, tools=tools, add_generation_prompt=False, tokenize=True)
+        if is_qwen2_5_vl:
+            from qwen_vl_utils import process_vision_info
+            prompt = tokenizer.apply_chat_template(messages, tools=tools, add_generation_prompt=False, tokenize=False)
+            images, videos = process_vision_info(messages)
+            model_input = processor(text=[prompt], images=images, videos=videos, return_tensors='pt')
+            tokens_without_prompt = model_input['input_ids'][0].tolist()
+
+            # update_visual_data
+            new_images = []
+            for msg in values["messages"]:
+                for ele in msg.content:
+                    if 'image' in ele:
+                        new_images.append(ele['image'])
+                    elif 'image_url' in ele:
+                        new_images.extend(ele['image_url'])
+            values['images'] = new_images
+
+            # pixel_values 和 image_grid_thw 在rollout中不会用到，是之后计算probs时用的
+            values['pixel_values'] = model_input.get('pixel_values', None)
+            values['image_grid_thw'] = model_input.get('image_grid_thw', None)
+
+        else:
+            tokens_without_prompt = tokenizer.apply_chat_template(messages, tools=tools, add_generation_prompt=False, tokenize=True)
+
         if not values.get("input_ids") or not values.get("attention_mask"):
-            tokenization_dict_with_prompt = tokenizer.apply_chat_template(messages, tools=[tool.model_dump() for tool in tool_schemas], add_generation_prompt=True, tokenize=True, return_dict=True)
-            values["input_ids"], values["attention_mask"] = tokenization_dict_with_prompt["input_ids"], tokenization_dict_with_prompt["attention_mask"]
+            if is_qwen2_5_vl:
+                # When using sglang to rollout online interaction data for qwen2_5_vl, the input_ids here are not used during rollout. sglang will preprocess + tokenize based on self.messages.
+                # However, when calculating probabilities (including action model and ref model), AutoModelForCausalLM will be directly fed with the input_ids and attention_mask here.
+                # Therefore, we must record the input_ids attention_mask loss_mask that correspond to the visual token length.
+                # The same applies to recording the corresponding input_ids for the length in self.add_user_response_messages.
+
+                prompt = tokenizer.apply_chat_template(messages, tools=[tool.model_dump() for tool in tool_schemas], add_generation_prompt=True, tokenize=False)
+                tokenization_dict_with_prompt = processor(text=[prompt], images=images, videos=videos)
+                values["input_ids"], values["attention_mask"] = tokenization_dict_with_prompt["input_ids"][0], tokenization_dict_with_prompt["attention_mask"][0]
+            else:
+                tokenization_dict_with_prompt = tokenizer.apply_chat_template(messages, tools=[tool.model_dump() for tool in tool_schemas], add_generation_prompt=True, tokenize=True, return_dict=True)
+                values["input_ids"], values["attention_mask"] = tokenization_dict_with_prompt["input_ids"], tokenization_dict_with_prompt["attention_mask"]
+
             if len(values["input_ids"]) > max_prompt_len:
                 # Only log the warning to avoid truncating in the middle of generation prompt. Consider raising an error for this case in the future.
                 logger.warning(f"Prompt {values['batch_data_id']} length {len(values['input_ids'])} greater than max_prompt_len {max_prompt_len} after applied chat template with tools.")
@@ -141,6 +192,31 @@ class AsyncRolloutRequest(BaseModel):
         assert len(self.input_ids) == len(self.attention_mask) == len(self.position_ids) == len(self.loss_mask), f"""Request {self.request_id} has different length of {len(self.input_ids)=}, 
             {len(self.attention_mask)=}, {len(self.position_ids)=}, {len(self.loss_mask)=}"""
 
+    def _update_visual_data(self, new_messages, model_input):
+        # extract all images from the new messages
+        new_images = []
+        for msg in new_messages:
+            for ele in msg.content:
+                if 'image' in ele:
+                    new_images.append(ele['image'])
+                elif 'image_url' in ele:
+                    new_images.extend(ele['image_url'])
+        self.images.extend(new_images)
+
+        if model_input is not None:
+            new_pixel_values = model_input.get('pixel_values', None)
+            new_image_grid_thw = model_input.get('image_grid_thw', None)
+            if new_pixel_values is not None:
+                self.pixel_values = torch.cat([self.pixel_values, new_pixel_values], dim=0) if self.pixel_values is not None else new_pixel_values
+            if new_image_grid_thw is not None:
+                self.image_grid_thw = torch.cat([self.image_grid_thw, new_image_grid_thw], dim=0) if self.image_grid_thw is not None else new_image_grid_thw
+
+    def get_image_data(self):
+        return self.images
+
+    def get_prompt(self, tokenizer: PreTrainedTokenizer) -> str:
+        return tokenizer.apply_chat_template([msg.model_dump() for msg in self.messages], add_generation_prompt=True, tokenize=False)
+
     def get_generation_prompt_ids(self, tokenizer: PreTrainedTokenizer) -> list[int]:
         generation_prompt_ids = [] if self.input_ids[-len(self.generation_prompt_ids) :] == self.generation_prompt_ids else self.generation_prompt_ids
         if generation_prompt_ids:
@@ -170,6 +246,30 @@ class AsyncRolloutRequest(BaseModel):
         content_ids = tokenizer.encode(content[self.base_conv_wo_gen_prompt_end_pos :], add_special_tokens=False)
         self._update_input_ids(content_ids, attention_mask=True, loss_mask=False)
 
+    def add_user_response_messages(self, processor: ProcessorMixin, contents: list[str]) -> None:
+        if not contents:
+            return
+        is_qwen2_5_vl = "Qwen2_5_VL" in processor.__class__.__name__
+
+        tokenizer = processor.tokenizer
+        self.messages.extend([Message(role="user", content=json.loads(content)) for content in contents])
+
+        # I suspect that apply_chat_template first adds BASE_CHAT_HISTORY, then removes the front part of the token ids,
+        # probably to clear out the system turn that some tokenizers forcibly include
+        new_messages = self.messages[-len(contents) :]
+        if is_qwen2_5_vl:
+            from qwen_vl_utils import process_vision_info
+            content = tokenizer.apply_chat_template([*BASE_CHAT_HISTORY, *new_messages], tools=([tool.model_dump() for tool in self.tool_schemas] if self.tool_schemas else None), add_generation_prompt=False, tokenize=False)
+            images, videos = process_vision_info([{'role': 'user', 'content': json.loads(content)} for content in contents])
+            model_input = processor(text=[content[self.base_conv_wo_gen_prompt_end_pos :]], images=images, videos=videos, return_tensors='pt')
+            content_ids = model_input['input_ids'][0].tolist()
+            self._update_visual_data(new_messages, model_input)
+        else:
+            content = tokenizer.apply_chat_template([*BASE_CHAT_HISTORY, *new_messages], tools=([tool.model_dump() for tool in self.tool_schemas] if self.tool_schemas else None), add_generation_prompt=False, tokenize=False)
+            content_ids = tokenizer.encode(content[self.base_conv_wo_gen_prompt_end_pos :], add_special_tokens=False)
+
+        self._update_input_ids(content_ids, attention_mask=True, loss_mask=False)
+
     def update_metrics(self, metrics: Any, tool_id: str) -> None:
         """
         metrics: should be a dict of tools_name -> Any
@@ -188,9 +288,10 @@ class AsyncRolloutRequest(BaseModel):
         self.reward_scores = reward_scores
         if self.enable_tokenization_sanity_check:
             full_tokens = tokenizer.apply_chat_template([msg.model_dump() for msg in self.messages], tools=([tool.model_dump() for tool in self.tool_schemas] if self.tool_schemas else None), add_generation_prompt=False, tokenize=True)
-            if self.input_ids != full_tokens:
-                logger.warning("Inconsistent training and inference tokenization detected. This may lead to unexpected behavior during training. Please review your chat template to determine if this is intentional. For more information, refer to the multiturn README.md.")
-                logger.info(f"Inference tokenization result:\n{tokenizer.decode(full_tokens, skip_special_tokens=False)}\ntraining content:\n{tokenizer.decode(self.input_ids, skip_special_tokens=False)}")
+            # this is intentional for qwen2_5vl tokenizer, no need to warn
+            # if self.input_ids != full_tokens:
+            #     logger.warning("Inconsistent training and inference tokenization detected. This may lead to unexpected behavior during training. Please review your chat template to determine if this is intentional. For more information, refer to the multiturn README.md.")
+            #     logger.info(f"Inference tokenization result:\n{tokenizer.decode(full_tokens, skip_special_tokens=False)}\ntraining content:\n{tokenizer.decode(self.input_ids, skip_special_tokens=False)}")
 
         # In case we failed to generate the assistant message and the generation prompt ids were already added to input_ids, remove them from the end of input_ids
         if self.input_ids[-len(self.generation_prompt_ids) :] == self.generation_prompt_ids:
diff --git a/verl/workers/rollout/sglang_rollout/sglang_rollout.py b/verl/workers/rollout/sglang_rollout/sglang_rollout.py
index 4e9c58e..6919600 100644
--- a/verl/workers/rollout/sglang_rollout/sglang_rollout.py
+++ b/verl/workers/rollout/sglang_rollout/sglang_rollout.py
@@ -25,6 +25,9 @@ from copy import deepcopy
 from json import JSONDecodeError
 from typing import List, Optional, Tuple
 from uuid import uuid4
+import aiohttp
+import requests
+import gc
 
 import numpy as np
 import sglang.srt.entrypoints.engine
@@ -63,6 +66,7 @@ from verl.tools.schemas import (
     OpenAIFunctionToolCall,
 )
 from verl.utils.debug import GPUMemoryLogger
+from verl.utils.device import get_torch_device
 from verl.utils.net_utils import is_ipv6
 from verl.utils.torch_functional import (
     get_response_mask,
@@ -226,6 +230,7 @@ class SGLangRollout(BaseRollout):
         port=None,
         trust_remote_code: bool = False,
         device_mesh: DeviceMesh | None = None,
+        processor=None,
         **kwargs,
     ):
         """Synchronized SGLang rollout engine.
@@ -252,6 +257,8 @@ class SGLangRollout(BaseRollout):
         """
         super().__init__()
         self.config = config
+        self.config.multi_turn.is_online_video_conversation = self.config.multi_turn.get("is_online_video_conversation", False)
+
         self._device_mesh_cpu = device_mesh
         os.environ.setdefault("SGL_DISABLE_TP_MEMORY_INBALANCE_CHECK", "true")
 
@@ -277,6 +284,7 @@ class SGLangRollout(BaseRollout):
         self._init_sampling_params(**kwargs)
 
         self.tokenizer = tokenizer
+        self.processor = processor
         self.pad_token_id = tokenizer.pad_token_id
 
     def _init_distributed_env(self, device_mesh_cpu, **kwargs):
@@ -320,16 +328,20 @@ class SGLangRollout(BaseRollout):
         self.visible_devices_set = set(",".join(visible_devices).split(","))
         os.environ["CUDA_VISIBLE_DEVICES"] = ",".join(sorted(list(self.visible_devices_set)))
 
+
     def _verify_config(self, model_hf_config):
         if not self.config.get("max_model_len", None):
             self.config.max_model_len = self.config.prompt_length + self.config.response_length
-        assert self.config.max_model_len >= self.config.prompt_length + self.config.response_length, f"""max_model_len should be greater than total sequence length (prompt_length + response_length): 
+        assert self.config.max_model_len >= self.config.prompt_length + self.config.response_length, f"""max_model_len should be greater than total sequence length (prompt_length + response_length):
             {self.config.max_model_len} >= {self.config.prompt_length} + {self.config.response_length}"""
         assert model_hf_config.max_position_embeddings >= self.config.max_model_len, "model context length should be greater than total sequence length"
         # currently max_turns stand for max number of tool calls
         if self.config.multi_turn.max_turns is None:
             self.config.multi_turn.max_turns = self.config.max_model_len // 3
 
+        if not self.config.get("max_new_tokens_per_generation", None):
+            self.config.max_new_tokens_per_generation = self.config.response_length
+
     def _init_inference_engine(self, trust_remote_code, actor_module, port):
         # initialize the inference engine
         nnodes = -(-self._tp_size // len(self.visible_devices_set))
@@ -344,6 +356,7 @@ class SGLangRollout(BaseRollout):
                 force_cpu_device=False,
             )
             dist_init_addr = f"[{ip}]:{port}" if is_ipv6(ip) else f"{ip}:{port}"
+            logger.info(f"running inference engine at: {dist_init_addr}")
         else:
             dist_init_addr = None
 
@@ -467,12 +480,22 @@ class SGLangRollout(BaseRollout):
         logger.info(f"Initialize tools from configuration.: tool_list: {tool_list}")
         tool_schemas = [tool.get_openai_tool_schema().model_dump() for tool in tool_list]
         tool_map = {tool.name: tool for tool in tool_list}
-        tool_call_parser_type = get_tool_call_parser_type(tokenizer)
         sgl_tools = [Tool.model_validate(tool_schema) for tool_schema in tool_schemas]
-        function_call_parser = FunctionCallParser(
-            sgl_tools,
-            tool_call_parser_type,
-        )
+
+        if config.multi_turn.is_online_video_conversation:
+            logger.info("using a always return True `ProvideOnlineVideoFunctionCallParser` as dummy function call parser")
+            from verl.tools.online_video_provider import ProvideOnlineVideoFunctionCallParser
+            tool_call_parser_type = 'always_true_tool_call_parser'
+            function_call_parser = ProvideOnlineVideoFunctionCallParser(
+                sgl_tools
+            )
+
+        else:
+            tool_call_parser_type = get_tool_call_parser_type(tokenizer)
+            function_call_parser = FunctionCallParser(
+                sgl_tools,
+                tool_call_parser_type,
+            )
 
         return (
             tool_schemas,
@@ -639,7 +662,6 @@ class SGLangRollout(BaseRollout):
 
         # users can customize different sampling_params at different run
         with self.update_sampling_params(**kwargs):
-            # print(f"{self.sampling_params=}")
             if self._tp_rank == 0:
                 loop = asyncio.get_event_loop()
                 output = loop.run_until_complete(
@@ -750,9 +772,24 @@ class SGLangRollout(BaseRollout):
                             for tool_call in parsed_tool_calls
                         ]
                     )
+
+                    if self.config.multi_turn.is_online_video_conversation:
+                        messages_from_users = [tool_call_result for tool_call_result, tool_call in zip(tool_call_results, parsed_tool_calls) if tool_call.function.name == 'online_video_provider']
+                        tool_call_results = [tool_call_result for tool_call_result, tool_call in zip(tool_call_results, parsed_tool_calls) if tool_call.function.name != 'online_video_provider']
+
                     _req.add_tool_response_messages(self.tokenizer, [resp for resp, _, _ in tool_call_results])
+
                     for tool_call, (resp, reward, metrics) in zip(parsed_tool_calls, tool_call_results):
                         _req.update_metrics(metrics, tool_call.function.name)
+
+                    if self.config.multi_turn.is_online_video_conversation:
+                        # here we use metric to record if there are more user turns to use as input
+                        if messages_from_users[0][2] == -1:
+                            finish_reason_type = FinishReasonTypeEnum.STOP
+                            break
+                        # if have more user turns, add them to user messages
+                        _req.add_user_response_messages(self.processor, [resp for resp, _, _ in messages_from_users])   # use processor instead of tokenizer as resp may contain video
+
                     if len(_req.input_ids) >= self.config.max_model_len:
                         finish_reason_type = FinishReasonTypeEnum.STOP
                         break
@@ -771,13 +808,20 @@ class SGLangRollout(BaseRollout):
                 current_turns += 1
                 if finish_reason_type == FinishReasonTypeEnum.LENGTH:
                     _req.add_assistant_message(self.tokenizer, content)
-                    break
+                    # If it is a multi-turn conversation, it is likely that the current turn length exceeds the length limit of each turn. This does not interrupt subsequent sampling.
+                    if not self.config.multi_turn.is_online_video_conversation:
+                        break
                 else:
                     if self._function_call_parser and self._function_call_parser.has_tool_call(content):
                         finish_reason_type = FinishReasonTypeEnum.TOOL_CALL
                         _req.state = AsyncRolloutRequestStateEnum.TOOL_CALLING
                         try:
-                            normed_content, tool_calls = self._function_call_parser.parse_non_stream(content)
+                            if not self.config.multi_turn.is_online_video_conversation:
+                                normed_content, tool_calls = self._function_call_parser.parse_non_stream(content)
+                            else:
+                                # we need to know which user turn is the next user turn to fetch. So here we pass conversation history to parse.
+                                # tool_calls always returns verl.tools.online_video_provider.OnlineVideoProvider tool call, which provide the next user turn.
+                                normed_content, tool_calls = self._function_call_parser.parse_non_stream(content, _req.messages)
                         except JSONDecodeError:
                             normed_content = content
                             tool_calls = []
@@ -812,6 +856,10 @@ class SGLangRollout(BaseRollout):
                         _req.add_assistant_message(self.tokenizer, content)
                         break
 
+                # Clear cache each time after a engine call
+                gc.collect()
+                get_torch_device().empty_cache()
+
         if current_turns >= self.config.multi_turn.max_turns:
             finish_reason_type = FinishReasonTypeEnum.STOP
 
@@ -835,7 +883,7 @@ class SGLangRollout(BaseRollout):
         generation_prompt_ids = _req.get_generation_prompt_ids(self.tokenizer)
         # Adjust max_new_tokens to ensure it is not greater than max_model_len - 1
         # SGLang raises an error when max_new_tokens + 1 is greater to max_model_len (the extra token accounts for the EOS token).
-        max_new_tokens = min(self.config.response_length, self.config.max_model_len - len(generation_prompt_ids) - 1)
+        max_new_tokens = min(self.config.max_new_tokens_per_generation, self.config.max_model_len - len(generation_prompt_ids) - 1)
         if not do_sample:
             kwargs = dict(
                 n=1,
@@ -847,7 +895,7 @@ class SGLangRollout(BaseRollout):
                 top_k=-1,
                 ignore_eos=False,
                 min_new_tokens=0,
-                max_new_tokens=self.config.response_length,
+                max_new_tokens=self.config.max_new_tokens_per_generation,
                 skip_special_tokens=True,
                 spaces_between_special_tokens=True,
             )
@@ -862,13 +910,24 @@ class SGLangRollout(BaseRollout):
         kwargs["max_new_tokens"] = max_new_tokens
         if "n" not in kwargs or (kwargs["n"] > 1 and override_n):  # group size is supported in preprocess
             kwargs["n"] = 1
-        # users can customize different sampling_params at different run
-        with self.update_sampling_params(**kwargs):
-            output = await self._engine.async_generate(
-                input_ids=generation_prompt_ids,
-                sampling_params=self.sampling_params,
-                return_logprob=False,
-            )
+
+        if not self.config.multi_turn.is_online_video_conversation:
+            # users can customize different sampling_params at different run
+            with self.update_sampling_params(**kwargs):
+                output = await self._engine.async_generate(
+                    input_ids=generation_prompt_ids,
+                    sampling_params=self.sampling_params,
+                    return_logprob=False,
+                )
+        else:
+            with self.update_sampling_params(**kwargs):
+                # For multi-modal input, we must enter input_text instead of input_ids and let the processor in sglang preprocess it by itself. So that the image can be loaded correctly and placed in the correct location
+                output = await self._engine.async_generate(
+                    prompt=_req.get_prompt(self.tokenizer),
+                    image_data=_req.get_image_data(),
+                    sampling_params=self.sampling_params,
+                    return_logprob=False,
+                )
         return output
 
     async def _handle_pending_state(self, _req: AsyncRolloutRequest) -> AsyncRolloutRequest:
@@ -910,16 +969,41 @@ class SGLangRollout(BaseRollout):
             )
             sorted_output_req_list = sorted(output_req_list, key=lambda x: (x.batch_data_id, x.rollout_offset))
         else:
-            sorted_output_req_list = None
+            # sorted_output_req_list = None
+            sorted_output_req_list = []
 
         dist.barrier()
-        [sorted_output_req_list] = broadcast_pyobj(
-            data=[sorted_output_req_list],
+        
+        # [sorted_output_req_list] = broadcast_pyobj(
+        #     data=[sorted_output_req_list],
+        #     rank=self._rank,
+        #     dist_group=self._device_mesh_cpu["tp"].get_group(),
+        #     src=self._device_mesh_cpu["tp"].mesh[0].item(),
+        #     force_cpu_device=False,
+        # )
+
+        # Broadcast the list of requests in batches to avoid CUDA OOM
+        list_length = len(sorted_output_req_list) if self._tp_rank == 0 else 0
+        [list_length] = broadcast_pyobj(
+            data=[list_length],
             rank=self._rank,
             dist_group=self._device_mesh_cpu["tp"].get_group(),
             src=self._device_mesh_cpu["tp"].mesh[0].item(),
             force_cpu_device=False,
         )
+
+        for i in range(list_length):
+            sorted_output_req_list_batch = sorted_output_req_list[i:i+1]
+            [sorted_output_req_list_batch] = broadcast_pyobj(
+                data=[sorted_output_req_list_batch],
+                rank=self._rank,
+                dist_group=self._device_mesh_cpu["tp"].get_group(),
+                src=self._device_mesh_cpu["tp"].mesh[0].item(),
+                force_cpu_device=False,
+            )
+            if self._tp_rank != 0:
+                sorted_output_req_list.extend(sorted_output_req_list_batch)
+
         # Construct the batch data
         prompt_ids, response_ids = [], []
         prompt_attention_mask, response_attention_mask = [], []
@@ -927,9 +1011,11 @@ class SGLangRollout(BaseRollout):
         prompt_loss_mask, response_loss_mask = [], []
         messages = []
         reward_scores = []
+        multi_modal_inputs = []
+
         for req in sorted_output_req_list:
             assert req.state == AsyncRolloutRequestStateEnum.COMPLETED, f"Request {req.request_id} is not completed"
-            assert len(req.input_ids) == len(req.attention_mask) == len(req.position_ids) == len(req.loss_mask), f"""Request {req.request_id} has different length of 
+            assert len(req.input_ids) == len(req.attention_mask) == len(req.position_ids) == len(req.loss_mask), f"""Request {req.request_id} has different length of
                 {len(req.input_ids)=}, {len(req.attention_mask)=}, {len(req.position_ids)=}, {len(req.loss_mask)=}"""
             error_message_lines = [
                 f"""Request {req.request_id} has input_ids length {len(req.input_ids)}
@@ -947,7 +1033,7 @@ class SGLangRollout(BaseRollout):
             response_ids.append(torch.tensor(req.response_ids, dtype=torch.int, device=tgt_device))
             if len(req.response_ids) > self.config.response_length:
                 logger.warning(
-                    f"""{req.request_id=} has response_ids length {len(req.response_ids)} 
+                    f"""{req.request_id=} has response_ids length {len(req.response_ids)}
                     greater than max_response_len {self.config.response_length},\n{req=}"""
                 )
             prompt_attention_mask.append(torch.tensor(req.prompt_attention_mask, dtype=torch.int, device=tgt_device))
@@ -958,6 +1044,7 @@ class SGLangRollout(BaseRollout):
             response_loss_mask.append(torch.tensor(req.response_loss_mask, dtype=torch.int, device=tgt_device))
             messages.append({"messages": req.messages})
             reward_scores.append(req.reward_scores)
+            multi_modal_inputs.append({'pixel_values': req.pixel_values, 'image_grid_thw': req.image_grid_thw})
 
         prompt_ids = pad_sequence(
             prompt_ids,
@@ -1023,6 +1110,7 @@ class SGLangRollout(BaseRollout):
             non_tensor_batch={
                 "messages": np.array(messages),
                 "reward_scores": np.array(reward_scores),
+                "multi_modal_inputs": np.array(multi_modal_inputs)      # subsequent frames in user turn loaded by tools here
             },
         )
 
@@ -1047,7 +1135,7 @@ class SGLangRollout(BaseRollout):
                     rollout_offset=rollout_offset,
                     request_id=str(uuid4()),
                     state=AsyncRolloutRequestStateEnum.PENDING,
-                    messages=raw_prompt.tolist(),
+                    messages=raw_prompt.tolist() if not isinstance(raw_prompt, list) else raw_prompt,
                     tool_schemas=_tool_schemas,
                     tools_kwargs=_tools_kwargs,
                     input_ids=_input_ids,
@@ -1062,7 +1150,7 @@ class SGLangRollout(BaseRollout):
                     max_model_len=min(self.config.max_model_len, self.config.prompt_length + self.config.response_length),
                     use_inference_chat_template=self.config.multi_turn.use_inference_chat_template,
                     enable_tokenization_sanity_check=self.config.multi_turn.enable_tokenization_sanity_check,
-                    tokenizer=self.tokenizer,
+                    tokenizer=self.tokenizer, processor=self.processor
                 )
 
                 error_message = f"Request {req.request_id} has mismatched lengths: input_ids={len(req.input_ids)}, attention_mask={len(req.attention_mask)}, position_ids={len(req.position_ids)}, loss_mask={len(req.loss_mask)}"
diff --git a/verl/workers/rollout/sglang_rollout/utils.py b/verl/workers/rollout/sglang_rollout/utils.py
index 0ae2b66..ba67002 100644
--- a/verl/workers/rollout/sglang_rollout/utils.py
+++ b/verl/workers/rollout/sglang_rollout/utils.py
@@ -20,7 +20,7 @@ import numpy as np
 import torch
 import torch.distributed as dist
 
-from verl.utils.device import get_device_name
+from verl.utils.device import get_device_name, get_torch_device
 
 
 def broadcast_pyobj(
@@ -42,16 +42,16 @@ def broadcast_pyobj(
         if len(data) == 0:
             tensor_size = torch.tensor([0], dtype=torch.long, device=device)
             dist.broadcast(tensor_size, src=src, group=dist_group)
+            return data
         else:
             serialized_data = pickle.dumps(data)
             size = len(serialized_data)
 
-            tensor_data = torch.ByteTensor(np.frombuffer(serialized_data, dtype=np.uint8)).to(device)
+            tensor_data = torch.ByteTensor(np.frombuffer(serialized_data, dtype=np.uint8).copy()).to(device)
             tensor_size = torch.tensor([size], dtype=torch.long, device=device)
 
             dist.broadcast(tensor_size, src=src, group=dist_group)
             dist.broadcast(tensor_data, src=src, group=dist_group)
-        return data
     else:
         tensor_size = torch.tensor([0], dtype=torch.long, device=device)
         dist.broadcast(tensor_size, src=src, group=dist_group)
@@ -65,4 +65,7 @@ def broadcast_pyobj(
 
         serialized_data = bytes(tensor_data.cpu().numpy())
         data = pickle.loads(serialized_data)
-        return data
+
+    # clear cache
+    del tensor_data
+    return data
diff --git a/verl/workers/sharding_manager/fsdp_sglang.py b/verl/workers/sharding_manager/fsdp_sglang.py
index 453967d..7ba143e 100644
--- a/verl/workers/sharding_manager/fsdp_sglang.py
+++ b/verl/workers/sharding_manager/fsdp_sglang.py
@@ -60,12 +60,14 @@ class FSDPSGLangShardingManager(BaseShardingManager):
         full_params: bool = False,
         device_mesh: DeviceMesh = None,
         offload_param: bool = False,
+        skip_gather_data_in_preprocess: bool = False,
     ):
         self.module = module
         self.inference_engine = inference_engine
         self.model_config = model_config
         self.device_mesh = device_mesh
         self.offload_param = offload_param
+        self.skip_gather_data_in_preprocess = skip_gather_data_in_preprocess
 
         # Full params
         self.full_params = full_params
@@ -218,7 +220,7 @@ class FSDPSGLangShardingManager(BaseShardingManager):
 
     def preprocess_data(self, data: DataProto) -> DataProto:
         """All gather across tp group to make each rank has identical input."""
-        if self.tp_size == 1:
+        if self.tp_size == 1 or self.skip_gather_data_in_preprocess:
             return data
 
         # TODO: Current impl doesn't consider FSDP with torch micro-dp
